/**
 * Operador ternario
 * Una forma reducida de hacer un if-else
 */

let x;

//por ejemplo tomar el valor de x o 90 por defecto
// si x existe, tomar el valor de 90
let rpta = x || 90;
console.log(rpta);

//

let edad = 20;
// condicion de verdad : condicion en caso no cumpla
let resultado = edad > 18 ? "mayor de edad" : "menor de edad";
console.log(resultado);

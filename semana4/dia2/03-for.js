/**
 *
 */

for (let c = 0; c < 15; c++) {
  console.log(`12 x ${c} = ${12 * c}`);
}

/**
 * crear un algoritmo para multiplicar 2 numeros
 * sin utilizar el signo de * (multiplicacion)
 */

let a = 10;
let b = 9;
let resultado = 0;

for (let c = 1; c <= a; c++) {
  resultado = resultado + b;
}
console.log(resultado);
